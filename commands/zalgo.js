/*const Zalgo = require ("to-zalgo");
module.exports = {
    name: "zalgo",
    description: "zalgo command",
    execute: async (bot, channelID, msg, args) => {
        main = require("../helperFunctions.js")
        await main.sendMsg(bot, channelID, Zalgo(args.join(" ")));
    }
}*/